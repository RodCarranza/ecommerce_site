</main>

    <style>
        /* Modernized Global Footer Layout */
        .main-footer {
            background-color: var(--bg-body);
            border-top: 1px solid var(--border-primary);
            padding: 32px 24px;
            text-align: center;
            font-size: 0.85rem;
            color: var(--text-muted);
            margin-top: auto; /* Keeps the footer at the bottom on short pages */
        }
    </style>

    <footer class="main-footer">
        <p>&copy; 2026 TechVault E-Commerce. Built securely via BYU CSE PostgreSQL Core.</p>
    </footer>
</body>
</html>